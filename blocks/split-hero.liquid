{% doc %}
  @prompt
    modern split-screen hero layout with a customizable text panel on the left and a full-width image panel on the right. The left side features a solid background, stacked headline, subtext, and a single CTA button, all left-aligned with generous padding. The right side displays a large edge-to-edge image that fills the entire vertical space of the section. Both panels are clean, minimal, and balanced, creating a clear visual hierarchy. The structure is designed to be flexible so you can swap in different images, adjust text content, change background colors, and update the CTA while keeping the overall layout consistent and visually strong., I want to be able to control if the image is on the left or right side. I also want to be able to control if the image is 1/3, 2/3, or 50% width. , The text panel controls has too many options. Are you able to either combine setting options, or remove some that are not needed so that the section is easier to use, add settings I can enable to make the text section Show below image on mobile and a setting to Center text  and button on mobile, The Show text below image on mobile toggle needs to be updated. Its doing the reverse of the intended functionality. Right now when I toggle it on, its moving the image below my text.
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-split-hero-{{ ai_gen_id }} {
    display: flex;
    width: 100%;
    min-height: {{ block.settings.section_height }}px;
    {% if block.settings.image_position == 'right' %}
      flex-direction: row;
    {% else %}
      flex-direction: row-reverse;
    {% endif %}
  }

  .ai-split-hero__panel-{{ ai_gen_id }} {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ai-split-hero__text-panel-{{ ai_gen_id }} {
    background-color: {{ block.settings.text_bg_color }};
    padding: 60px;
    {% if block.settings.image_width == '33' %}
      flex: 0 0 66.666%;
    {% elsif block.settings.image_width == '66' %}
      flex: 0 0 33.333%;
    {% else %}
      flex: 0 0 50%;
    {% endif %}
  }

  .ai-split-hero__text-content-{{ ai_gen_id }} {
    max-width: 600px;
    text-align: left;
  }

  .ai-split-hero__heading-{{ ai_gen_id }} {
    color: {{ block.settings.heading_color }};
    font-size: 48px;
    line-height: 1.2;
    margin: 0 0 24px 0;
    font-weight: 700;
  }

  .ai-split-hero__subtext-{{ ai_gen_id }} {
    color: {{ block.settings.text_color }};
    font-size: 18px;
    line-height: 1.5;
    margin: 0 0 32px 0;
  }

  .ai-split-hero__button-{{ ai_gen_id }} {
    display: inline-block;
    padding: 14px 32px;
    background-color: {{ block.settings.button_bg_color }};
    color: {{ block.settings.button_text_color }};
    text-decoration: none;
    border-radius: 2px;
    font-size: 16px;
    font-weight: 600;
    transition: background-color 0.3s ease;
    border: 2px solid {{ block.settings.button_bg_color }};
  }

  .ai-split-hero__button-{{ ai_gen_id }}:hover {
    background-color: {{ block.settings.button_hover_bg_color }};
    border-color: {{ block.settings.button_hover_bg_color }};
    color: {{ block.settings.button_text_color }};
  }

  .ai-split-hero__image-panel-{{ ai_gen_id }} {
    position: relative;
    overflow: hidden;
    {% if block.settings.image_width == '33' %}
      flex: 0 0 33.333%;
    {% elsif block.settings.image_width == '66' %}
      flex: 0 0 66.666%;
    {% else %}
      flex: 0 0 50%;
    {% endif %}
  }

  .ai-split-hero__image-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .ai-split-hero__image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ai-split-hero__image-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 400px;
    max-height: 400px;
  }

  .ai-split-hero__empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    padding: 12px 20px;
    font-size: 14px;
    color: #666;
    text-align: center;
    pointer-events: none;
  }

  @media screen and (max-width: 749px) {
    .ai-split-hero-{{ ai_gen_id }} {
      {% if block.settings.mobile_text_below %}
        flex-direction: column-reverse;
      {% else %}
        flex-direction: column;
      {% endif %}
      min-height: auto;
    }

    .ai-split-hero__text-panel-{{ ai_gen_id }},
    .ai-split-hero__image-panel-{{ ai_gen_id }} {
      flex: 0 0 100%;
    }

    .ai-split-hero__text-panel-{{ ai_gen_id }} {
      padding: 40px 24px;
    }

    .ai-split-hero__text-content-{{ ai_gen_id }} {
      {% if block.settings.mobile_center_text %}
        text-align: center;
        margin-left: auto;
        margin-right: auto;
      {% endif %}
    }

    .ai-split-hero__heading-{{ ai_gen_id }} {
      font-size: 32px;
    }

    .ai-split-hero__subtext-{{ ai_gen_id }} {
      font-size: 16px;
    }

    .ai-split-hero__image-panel-{{ ai_gen_id }} {
      min-height: 300px;
    }
  }
{% endstyle %}

<div class="ai-split-hero-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-split-hero__panel-{{ ai_gen_id }} ai-split-hero__text-panel-{{ ai_gen_id }}">
    <div class="ai-split-hero__text-content-{{ ai_gen_id }}">
      {% if block.settings.heading != blank %}
        <h2 class="ai-split-hero__heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h2>
      {% endif %}

      {% if block.settings.subtext != blank %}
        <div class="ai-split-hero__subtext-{{ ai_gen_id }}">{{ block.settings.subtext }}</div>
      {% endif %}

      {% if block.settings.button_text != blank %}
        <a
          href="{{ block.settings.button_link }}"
          class="ai-split-hero__button-{{ ai_gen_id }}"
        >
          {{ block.settings.button_text }}
        </a>
      {% endif %}
    </div>
  </div>

  <div class="ai-split-hero__panel-{{ ai_gen_id }} ai-split-hero__image-panel-{{ ai_gen_id }}">
    {% if block.settings.image %}
      <img
        src="{{ block.settings.image | image_url: width: 2000 }}"
        alt="{{ block.settings.image.alt | escape }}"
        loading="lazy"
        width="{{ block.settings.image.width }}"
        height="{{ block.settings.image.height }}"
        class="ai-split-hero__image-{{ ai_gen_id }}"
      >
    {% else %}
      <div class="ai-split-hero__image-placeholder-{{ ai_gen_id }}">
        {{ 'hero-apparel-1' | placeholder_svg_tag }}
        <div class="ai-split-hero__empty-state-{{ ai_gen_id }}">
          Next, add an image
        </div>
      </div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Split-screen hero",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "section_height",
      "min": 300,
      "max": 800,
      "step": 50,
      "unit": "px",
      "label": "Minimum height",
      "default": 600
    },
    {
      "type": "select",
      "id": "image_position",
      "label": "Image position",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "right"
    },
    {
      "type": "select",
      "id": "image_width",
      "label": "Image width",
      "options": [
        {
          "value": "33",
          "label": "1/3"
        },
        {
          "value": "50",
          "label": "1/2"
        },
        {
          "value": "66",
          "label": "2/3"
        }
      ],
      "default": "50"
    },
    {
      "type": "header",
      "content": "Mobile layout"
    },
    {
      "type": "checkbox",
      "id": "mobile_text_below",
      "label": "Show text below image on mobile",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "mobile_center_text",
      "label": "Center text on mobile",
      "default": false
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "richtext",
      "id": "heading",
      "label": "Heading",
      "default": "<p>Welcome to our store</p>"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "Subtext",
      "default": "<p>Discover our curated collection of premium products designed to elevate your everyday life.</p>"
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Shop now"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "text_bg_color",
      "label": "Background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "heading_color",
      "label": "Heading",
      "default": "#006c46"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#222222"
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "Button background",
      "default": "#006c46"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "Button text",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "button_hover_bg_color",
      "label": "Button hover",
      "default": "#00856a"
    },
    {
      "type": "header",
      "content": "Image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "presets": [
    {
      "name": "Split-screen hero"
    }
  ]
}
{% endschema %}
