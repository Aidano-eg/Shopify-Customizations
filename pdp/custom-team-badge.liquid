{%- comment -%}
Auto-detects the first team collection the product belongs to
and renders the "Officially Licensed Gear" badge with the collection image.
A "team collection" is simply the first collection the product belongs to
that has an image set (your team logo).
Add to page with {% render 'custom-team-badge' %}
{%- endcomment -%}

{%- assign team_collection = blank -%}

{%- for c in product.collections -%}
  {%- if c.image != blank and c.handle != 'collegiate' -%}
    {%- assign team_collection = c -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}

{%- if team_collection -%}
  <div class="tsa-team-badge" role="note" aria-label="Officially Licensed Gear">
    <img
      class="tsa-team-badge__logo"
      src="{{ team_collection.image | image_url: width: 80 }}"
      alt="{{ team_collection.title | escape }} logo"
      loading="lazy"
      width="80px"
      height="80px"
    >
    <span class="tsa-team-badge__text">Officially Licensed Gear</span>
  </div>
{%- endif -%}

<style>
.tsa-team-badge {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  margin-bottom: 0.5rem;
}
.tsa-team-badge__logo {
  height: 40px;
  width: auto;
  object-fit: contain;
}
.tsa-team-badge__text {
  font-size: 16px;
  line-height: 1;
  color: #1a1a1a;
  font-weight: 500;
}
<style>
